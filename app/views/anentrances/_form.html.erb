<%= form_for(@anentrance) do |f| %>
  <% if @anentrance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anentrance.errors.count, "error") %> prohibited this anentrance from being saved:</h2>

      <ul>
      <% @anentrance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
