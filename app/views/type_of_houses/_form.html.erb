<%= form_for(@type_of_house) do |f| %>
  <% if @type_of_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_of_house.errors.count, "error") %> prohibited this type_of_house from being saved:</h2>

      <ul>
      <% @type_of_house.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
