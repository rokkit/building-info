<h1>Listing type_of_houses</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @type_of_houses.each do |type_of_house| %>
  <tr>
    <td><%= type_of_house.name %></td>
    <td><%= link_to 'Show', type_of_house %></td>
    <td><%= link_to 'Edit', edit_type_of_house_path(type_of_house) %></td>
    <td><%= link_to 'Destroy', type_of_house, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Type of house', new_type_of_house_path %>
