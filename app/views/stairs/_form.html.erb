<%= form_for(@stair) do |f| %>
  <% if @stair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stair.errors.count, "error") %> prohibited this stair from being saved:</h2>

      <ul>
      <% @stair.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
